using UnityEngine;

public class AB : MonoBehaviour
{
    private AssetBundle a;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int a=123;
        a.GetType();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
